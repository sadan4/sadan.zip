diff --git a/dist/esm/vite/start-manifest-plugin/normalized-client-build.js b/dist/esm/vite/start-manifest-plugin/normalized-client-build.js
index 2ad432136644dbaa439921c9c5d914438ed13264..ac0abebeb1b905c7be9cd2081ecaa4a5be726b73 100644
--- a/dist/esm/vite/start-manifest-plugin/normalized-client-build.js
+++ b/dist/esm/vite/start-manifest-plugin/normalized-client-build.js
@@ -1,5 +1,6 @@
 import { getCssAssetSource } from "../../start-manifest-plugin/inlineCss.js";
 import { tsrSplit } from "@tanstack/router-plugin";
+import { resolve, normalize } from "node:path";
 //#region src/vite/start-manifest-plugin/normalized-client-build.ts
 function normalizeViteClientChunk(chunk) {
 	return {
@@ -29,7 +30,8 @@ function normalizeViteClientBuild(clientBundle) {
 	const cssContentByFileName = /* @__PURE__ */ new Map();
 	for (const chunk of chunksByFileName.values()) {
 		const bundleEntry = clientBundle[chunk.fileName];
-		if (chunk.isEntry) {
+		// hack to get it to recognize our entry chunk
+		if (chunk.isEntry && chunk.fileName.startsWith("assets/")) {
 			if (entryChunkFileName) throw new Error(`multiple entries detected: ${entryChunkFileName} ${chunk.fileName}`);
 			entryChunkFileName = chunk.fileName;
 		}
