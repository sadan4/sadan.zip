name: Nightly Rust Binaries

on:
  push:
    branches:
      - web
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/**"
      - ".github/workflows/nightly-binaries.yml" # lol

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            ext: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            ext: ".exe"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Setup pnpm
        run: corepack enable pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo artifacts
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build release binaries
        run: >
          cargo build --release --target ${{ matrix.target }}
          -p reporter -p explorer_server -p companion_lsp -p pretty_printer

      - name: Prepare release files
        shell: bash
        run: |
          mkdir -p release
          cp "target/${{ matrix.target }}/release/reporter${{ matrix.ext }}" "release/reporter-${{ matrix.target }}${{ matrix.ext }}"
          cp "target/${{ matrix.target }}/release/explorer_server${{ matrix.ext }}" "release/explorer_server-${{ matrix.target }}${{ matrix.ext }}"
          cp "target/${{ matrix.target }}/release/companion_lsp${{ matrix.ext }}" "release/companion_lsp-${{ matrix.target }}${{ matrix.ext }}"
          cp "target/${{ matrix.target }}/release/pretty_printer${{ matrix.ext }}" "release/pretty_printer-${{ matrix.target }}${{ matrix.ext }}"

      - name: Package extension
        run: cargo xtask package extension

      - name: Move extension to release
        shell: bash
        run: |
          mv packages/VencordCompanion/*.vsix "release/vencord-user-companion-${{ matrix.target }}.vsix"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.target }}
          path: release/*
          if-no-files-found: error

  release:
    name: Publish nightly release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          pattern: binaries-*
          merge-multiple: true

      - name: Publish or update nightly release
        uses: ncipollo/release-action@v1
        with:
          tag: nightly
          name: Nightly
          prerelease: true
          allowUpdates: true
          removeArtifacts: true
          replacesArtifacts: true
          makeLatest: false
          artifacts: release-assets/*
